server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # React 单页应用:任意路径回落到 index.html(深链接/刷新不 404)
  location / {
    try_files $uri $uri/ /index.html;
  }

  # 带内容哈希的构建产物,长缓存
  location /assets/ {
    expires 7d;
    add_header Cache-Control "public, immutable";
  }
}
